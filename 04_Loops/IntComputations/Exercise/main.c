#include <stdint.h>
#include <stdio.h>

int main()
{
    uint32_t number = 0;

    printf("Please enter a unsinged integer: ");
    scanf("%u", &number);

    // sum_of_digits

    printf("sum_of_digits: %u\n", num_digits);

    // cross_sum

    printf("cross_sum: %d\n", cross_sum);
}
